function create(){
    let btn = document.createElement('button');
    btn.innerText = "question";
    document.body.appendChild(btn);
}
// line two creates a button element
function read(){
    let btn = document.readyState('button');
    btn.innerText = 'meet';
    document.body.appendChild(btn)
}